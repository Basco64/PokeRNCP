# =====================
# Database
# =====================
# When using docker-compose, the Postgres service is named `db`.
DATABASE_URL=postgres://rncp:rncp@db:5432/rncp

# =====================
# JWT / Auth secrets
# =====================
JWT_SECRET=change-me
JWT_REFRESH_SECRET=change-me-too
RESET_SECRET=change-me-reset
# Optional TTLs (defaults exist in code)
# JWT_EXP_SECONDS=900
# JWT_REFRESH_EXP_SECONDS=2592000
# RESET_TOKEN_EXP_SECONDS=3600

# =====================
# Runtime flags
# =====================
# In container we serve frontend + backend from the same origin
PRODUCTION_MODE=false
# FRONTEND_ORIGIN can stay same-origin when using the container
FRONTEND_ORIGIN=http://localhost:8080
# BACKEND_URL is required by the backend, the Docker image sets a default
# BACKEND_URL=0.0.0.0:8080
